{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": [],
      "outputs": []
    },
    "test": {
      "dependsOn": [],
      "outputs": [],
      "inputs": ["src/**/*.test.tsx", "src/**/*.test.ts"],
      "cache": false
    },
    "pre-deploy": {
      "dependsOn": ["build", "lint", "test"],
      "outputs": []
    }
  }
}
